# possible-routes
Several experiments inside the repository focus on transaction movement between independent environments.

Some examples study possible routes that execution requests may follow during testing.

Other utilities compare behavior across different configurations and runtime conditions.

The concept of liquidity also appears in development notes where transaction movement and execution timing were evaluated during local experiments.
